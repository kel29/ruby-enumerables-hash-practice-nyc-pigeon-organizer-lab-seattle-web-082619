def nyc_pigeon_organizer(data)
  # pull pidgeon names from gender 
  puts data[:gender].each
  # add each pidgeon as a key in a new has 
  # iterate through the rest of the items to get the info into the new hash
end
